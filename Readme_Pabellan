This activity helped me understand when to use parallel algorithms. It depends on the size of the data and the type of problem.

For small datasets, sequential is better because it has less overhead. For large datasets, parallel can be faster, like what we saw in sorting.

However, for searching, parallel didn’t help much in our case. It was slower in all tests. So I learned that parallelism should be used only when necessary and not just because it is available.